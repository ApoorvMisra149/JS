//Different data types
age=20
first_name="Apoorv"
is_Online= False

//Input Fnc
name= input("What is your name?") //input func returns value as a string
print("Hello" + name) //python only concatenates the string type func

//Data type conversion
birth_year = input("Enter your birth year")
age= 2024 - int(birth_year) //converting birth_year from string type to integer type
int() float() bool() str() //different data types to convert values

//or we can also convert the data type while inputing , example-
abc= float(input("Select a number"))

//String func
course = 'Python for Beginners' //string can be in '' or ""
print(course.upper())
print(course.find('y'))
print('Python' in course)// returns True, in is a built-in function

//And-Or operator ---- and, or

//if-else : in python we don't use curly braces for scope & round braces for condition
if temperature >30:
    print("Hot day")
elif temperature > 20:
    print("Good weather")
else:
    print("Cold")

    //For loop
    numbers = [1,2,3,4,5]
    for item in numbers: //no need to declare item
        print(item)

//Range function
for x in range(6)
print(x)  //output- 1 to 5
//The range() function returns a sequence of numbers, starting from 0 by default, and increments by 1 (by default). Note that range(6) is not the values of 0 to 6, but the values 0 to 5.

number = range(5,10,2)// store number starting from 5 to 9 with a gap of 2 ...output- 5,7,9
for x in range(5,10,2):

//List, Array
x=[1,2,3,4,5]
x.insert(0,1)
print(x)